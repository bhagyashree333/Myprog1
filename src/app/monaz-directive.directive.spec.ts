import { MonazDirectiveDirective } from './monaz-directive.directive';

describe('MonazDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MonazDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
